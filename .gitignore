# Local publish config may hold environment-specific paths; PAT is always in env.
publish/config.toml

# Build intermediates + exported previews + generated Pages site
build/
site/
*.hyper
hyperd.log

# Secrets (never commit)
secrets/
.env
.env.*

# Python
__pycache__/
*.pyc
